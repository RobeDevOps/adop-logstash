input {
	beats {
    	port => 5055
	}
}

output {
    if [metricset][module] == "docker" {
        if [metricset][name] == "info" {
            elasticsearch {
                hosts => [ "${ELASTICSEARCH_URL}"]
                index => "adop_info_%{+YYYY.MM.dd}"
            }
        }
    }
}